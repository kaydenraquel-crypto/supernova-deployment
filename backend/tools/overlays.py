from typing import List, Dict, Any

def draw_overlays(overlays: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """
    Process overlay data for chart rendering.
    This is a no-op function as the UI will handle rendering.
    Keep for symmetry/tooling consistency.
    """
    return overlays